@model IEnumerable<ITI_Project.Models.JobTitle>

@{
    ViewData["Title"] = "Job Titles";
}

<h2>Job Titles</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">+ Add New Job Title</a>
</p>

<form asp-action="Index" method="get" class="row g-3 mb-4">
    <div class="col-md-4">
        <label class="form-label">Search</label>
        <input type="text" name="searchString" value="@ViewBag.CurrentSearch"
               class="form-control" placeholder="Write job title..." />
    </div>
    <div class="col-md-2 d-flex align-items-end">
        <button type="submit" class="btn btn-outline-primary me-2">Search</button>
        <a asp-action="Index" class="btn btn-outline-secondary">Reset</a>
    </div>
</form>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Job Title</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Any())
        {
            @foreach (var job in Model)
            {
                <tr>
                    <td>@job.Title</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@job.Id" class="btn btn-sm btn-info">Details</a>
                        <a asp-action="Edit" asp-route-id="@job.Id" class="btn btn-sm btn-warning">Edit</a>
                        <a asp-action="Delete" asp-route-id="@job.Id" class="btn btn-sm btn-danger">Delete</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="2" class="text-center">No job titles found.</td>
            </tr>
        }
    </tbody>
</table>